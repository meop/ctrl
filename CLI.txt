ctrl:
    self configuring initial state
        detect
            os
            shell
        save
    auto update checks or 
        git check location

API
    asset
        cfg
        bak
        dot
        pkg
            group

ctrl

___
API
    git
        check tags
    github
        check release

___
CLI
    config
        bak
            provider
                rclone
                rsync
                cp
        dot
            tool
                git
        pkg
            tool
    backup [--compress] [--diff]
    dotfile <plug>
        g | get
        p | put
    os
        font
        registry [windows only]
    package
        a | add
        l | list
        m | match
        r | remove [--purge]
        s | search
        u | upgrade [--nosync]
    update



dot plugs
    tmux
        tmux.conf
        tpm
    vim
        vimrc
        vim-plug
    zsh



if multiple choices
"_x_ provider not found.. setup: "

if only one choice
"_x_ is not installed.. install? [Y/n] "

if not supported
"_x_ is not supported on _y_"
